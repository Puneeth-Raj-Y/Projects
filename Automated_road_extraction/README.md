# 🛰️ Automated Road Extraction from Satellite Images

This project uses Machine Learning (SVM & XGBoost) to identify roads in satellite imagery.

## 🚀 How to Run the App

1.  **Open Terminal** in the project folder:
    ```bash
    cd c:\mca\road-extraction
    ```

2.  **Start the Server**:
    Run the following command:
    ```bash
    python -m streamlit run app.py
    ```

3.  **Access the App**:
    👉 Open your browser to: **[http://localhost:8501](http://localhost:8501)**

## 📂 Data Setup

The dataset has been organized into Training and Testing sets.
*   **Training Data**: `c:\mca\road-extraction\data\train`
*   **Testing Data**: `c:\mca\road-extraction\data\test`

When using the "Real Data" mode in the app, these paths are pre-filled for convenience.
